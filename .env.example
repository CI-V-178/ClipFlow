# ClipFlow MVP - 環境変数テンプレート
#
# 本アプリは BYOAI (Bring Your Own AI Key) 方式を採用しており、
# Gemini API キーはブラウザの LocalStorage で管理されます。
# サーバー側にキーは保存しないため、現時点で必須の環境変数はありません。
#
# 値を設定する場合は本ファイルをコピーして `.env.local` を作成してください。
#   cp .env.example .env.local
