/**
 * ClipFlow MVP 型定義
 */

// =============================================================================
// API Keys (BYOAI / LocalStorage)
// =============================================================================
export interface ApiKeys {
  gemini?: string;
}

// =============================================================================
// 切り抜き候補 (仕様 §3.4 見どころ抽出)
// =============================================================================
export interface ClipCandidate {
  /** 開始秒 */
  startSec: number;
  /** 終了秒 */
  endSec: number;
  /** 候補タイトル（フック） */
  title: string;
  /** なぜ切り抜き候補としたか（理由） */
  reason: string;
}
